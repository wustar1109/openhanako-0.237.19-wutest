import redactor, {
  redactLogText,
  redactLogValue,
  redactLogLabel,
  formatLogArgs,
} from "../shared/log-redactor.js";

export {
  redactLogText,
  redactLogValue,
  redactLogLabel,
  formatLogArgs,
};

export default redactor;
